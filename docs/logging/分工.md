# Logging V1 · 两人分工

依据：[README.md](./README.md) 第 3 节两个边界。

**线上进度（2026-09-08）：**

| 阶段 | 提交 | 是否已在 `origin/dev` |
| --- | --- | --- |
| P1 Core + 认领带头 | `405d1f0`、`0cd207b` | 是（PR [#4](https://github.com/MKC-ShenZhi/Shenzhi-ResearchOS/pull/4)） |
| P2 外部服务 completed/failed | `e95798a` | 是（随 PR #4 一并合入） |
| P3/P4 流终态 + 错误气泡 `request_id` | `789ee2f` | 是（PR [#10](https://github.com/MKC-ShenZhi/Shenzhi-ResearchOS/pull/10) 已合） |

Chat 认领 fixup（PR [#1](https://github.com/MKC-ShenZhi/Shenzhi-ResearchOS/pull/1)）已合入。不要平行新建 `feat/logging-core`，不要重写已推的 P1–P4。

---

## 郑子轩（Chat / 走查 / 验收）

守 **边界二（Chat 流）** 和主路径。P3/P4 **代码已由陶俊铭写在 `feat/logging-v1`**，这里不再重写 `chat.py` / 错误气泡。

| 顺序 | 做什么 | 状态 |
| --- | --- | --- |
| 1 | 按 `docs/chat/BROWSER-WALKTHROUGH.md` 走主路径 | API：匿名关知识库提问 **通过**（`done`，正文有内容，侧栏时间按秒为 2026）。浏览器登录 / 暂不迁移 / 迁移 / 账号 B **未测** |
| 2 | P1 已合入：单次 HTTP 的 BFF 响应带 `X-Request-ID` | **通过**（config / create / stream 各有自己的 ID，符合一轮多请求）。Backend 窗口是否同一 ID **需你在 Backend 黑窗搜** |
| 3–4 | 审 PR #10：P3 只旁路日志、P4 不改 `{code,message}` | 已合入 `dev`（`112476d`）。本机自动测：Web 147 通过；Backend 110 passed / 18 skipped |
| 5 | Stop / 失败流，用 `message_id` 对上多条 `request_id` | **待浏览器**（登录 / 迁移需真人两账号） |
| 6 | 错误气泡展示并可复制 `request_id` 的自动测 | Web `tests/chat/error-bubble.test.ts`；Backend 流终态日志已在 `test_chat_api.py` |

陶俊铭本机「BFF 挡真实提问」是**凭据策略，不是 Logging Bug**：`/api/v1` 在没有 `BACKEND_BFF_SECRET`、且未显式 `BACKEND_ALLOW_INSECURE_LOCAL_BFF=true`（仅 loopback）时直接 503 `10001`，请求到不了 FastAPI。不要为走查放宽这段 fail-closed。郑子轩本机两端 Secret 已配，由这边做浏览器验收。

---

## 陶俊铭（P1→P2，并已做完 P3/P4 实现）

| 顺序 | 做什么 | 状态 |
| --- | --- | --- |
| 1–3 | 开 PR 合 P1；认领路由带头 | 完成（#4、`0cd207b`） |
| 4–5 | P2 外部出口日志；Knowledge `requestId` 走 ContextVar | 完成（`e95798a`，已在 `dev`） |
| 额外 | P3/P4 实现 | 完成，已合 #10 |

不改：认领弹窗文案、Auth / DB、业务 retry/fallback。

---

## 交接（原则未变）

- Logging Core 不理解业务；只观察，不决定 retry / fallback / HTTP status / 状态机 / 返回值。
- 普通 HTTP：不要为打日志 catch 再 raise；traceback 只在最终边界一次。
- Chat 流：保留现有 except/finally，终态旁路 `chat.stream.*`，不新套一层。
- 有业务决策 → 边界二，catch 写完备；只想留痕 → 边界一。
- 争议以已合入的 Phase 成果为准，不另起平行 Core。
